# Homework 1

## Question 1

Create a Markdown cell with the headline `QESTION1`.
The cell will present another two lines:
- the first one will be your highlighted first name
- the second one will be your last name in red color

## Question 2

Create a Markdown cell with a smaller headline `QUESTION2`.
The cell will present another two lines:
- the first one will be your phone number in bold
- the second one will present your home address in bold and italic mode

## Question 3

Create a Markdown cell with a smaller headline `QUESTION3`.
The cell will print the required pyramid by using the indenting tab.

## Question 4

Create a Markdown cell with the headline `QUESTION4`.
The cell will present the Pythagorean Theorem.

## Question 5

Create a Code cell.
Create a comment in the beginning of the cell `QUESTION5`.
Create a variable named `x` that will hold the value `3`.
Create a variable named `y` that will hold the value `3.141`.
Create a variable named `AREA` that will hold the result of `y * x ^ 2`.
Print the values of `x`, `y`, and `AREA`.
Print the types of `x`, `y`, and `AREA`.

## Question 6

Create a Code cell.
Create a comment at the beginning of the cell `QUESTION6`.
Print the value of `AREA // 4 <= 7`.
Print the value of `AREA / 4 < 7`.
Print the value of:
`not AREA / 4 < 7 and AREA // 4 <= 7`
Use the variable `AREA` from the previous cell.

## Question 7

Create a Code cell.
Create a comment at the beginning of the cell `QUESTION7`.
Create a variable named `str_name` that will hold your full name, first name, and last name separated by a space.
Print all values in the even indexes.
Print all values in the odd indexes.

## Question 8

Create a Code cell.
Create a comment at the beginning of the cell `QUESTION8`.
Create a variable named `a` that will hold the value `#####`.
Create a variable named `b` that will hold the value `*****`.
Create a variable named `c` that will hold your name.
Concatenate the variables in the following order: `a`, `c`, `b`.
Use the correct arithmetic operator between the variables.
Find the index of the space character; you can use the `.find()` method.
Use the index of the space that you found and print in an f-string the length of the first name and the length of the last name.

## Question 9

Create a Code cell.
Create a comment at the beginning of the cell `QUESTION9`.
Use the matplotlib example that was shown in the lecture to solve the next question.
Print two red circles that touch each other as shown in the example.